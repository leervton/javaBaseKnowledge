src 存放着未编译的手写源码，是程序的源码，也可以认为是程序本身，只不过需要进行编译

bin src文件的源码进行编译生成.class字节码文件存放的地址，说的直白点，src等同于bin，只不过src是源码，bin是字节码文件
bin 存放着软件管理命令文件夹，例如启动，关闭命令

config：核心配置文件夹

lib：依赖包

log：日志

temp：临时文件

webapps：默认资源文件夹，例如tomcat服务器默认的网站都放置在这里

docs 帮助文档

entity 数据库对应的数据类

VO（view Object）视图层对象，从网页页面接受的对象

bean 普通类，javabean，bean是豌豆荚的意思，将对象像豌豆一样封装保存起来

dao 数据库操作类，用于编写增删改查方法的操作类

util 工具包类

controller 从前端收到数据，决定调用何种后端处理程序的中央调度分发控制类

impl 接口的实现类

POJO(Plain Ordinary Java Object） 简单普通的java对象，一般用于数据层映射到数据库表的类

PO(Persistant Object) 持久化对象，可以理解为数据库中的一条数据，同时也可理解为POJO经过持久化的对象

BO(Business Object) 业务对象，一般用于业务层，当业务比较复杂的时候，多使用此对象封装多个对象进行传递

DTO(Data Transfer Object) 数据传输对象，一般用于向数据外层提供仅需的数据，例如：一个表有20个字段，但是服务只需要其中5个，就可以封装一个DTO。这样做可以隐藏数据层的定义，同时提高系统性能，较少无用字段传输的性能损耗

